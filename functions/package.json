{
  "name": "ai-exercise-manager-functions",
  "private": true,
  "version": "1.0.0",
  "description": "Server-side Claude Evaluator and account provisioning for the AI Skills Exercise Manager.",
  "//main": "rootDir is the repo root so the shared src/ modules compile too — see tsconfig.json.",
  "main": "lib/functions/src/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "lint": "tsc -p tsconfig.json --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions,auth,database"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
